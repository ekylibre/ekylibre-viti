/*
 * Leaflet.Icon.SensorDefault is the blue marker icon with white 'wi-fi' symbol used
 * by default for sensor positions. It behaves like DefaultIcon on all other accounts.
 */

Leaflet.Icon.SensorDefault = Leaflet.Icon.Default.extend({
  _getIconUrl: function (name) {
    var key = name + 'Url';

    if (this.options[key]) {
      return this.options[key];
    }

    if (Leaflet.Browser.retina && name === 'icon') {
      name += '-2x';
    }

    if (Leaflet.Browser.retina && name === 'icon') {
      return "<%= asset_path('map/marker/sensor-2x.png') %>";
    }
    if (name === 'shadow') {
      return "<%= asset_path('map/marker/shadow.png') %>";
    } else {
      return "<%= asset_path('map/marker/sensor.png') %>";
    }


    return path + '/marker-' + name + '.png';
  }
});